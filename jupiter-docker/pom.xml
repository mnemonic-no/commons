<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>jupiter-docker</artifactId>
  <packaging>jar</packaging>

  <name>mnemonic commons - jupiter docker</name>
  <description>JUnit Jupiter extensions to use Docker containers during integration tests</description>

  <parent>
    <artifactId>parent</artifactId>
    <groupId>no.mnemonic.commons</groupId>
    <version>0.4.0</version>
  </parent>

  <dependencies>
    <dependency>
      <groupId>no.mnemonic.commons</groupId>
      <artifactId>utilities</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.mandas</groupId>
      <artifactId>docker-client</artifactId>
      <version>${docker.client.version}</version>
      <classifier>shaded</classifier>
    </dependency>
    <!--
        docker-client removed its dependency on javax.activation but without it Jersey throws a ClassNotFoundException.
        Temporarily add it back until this is fixed upstream (https://github.com/dmandalidis/docker-client/issues/172).
    -->
    <dependency>
      <groupId>javax.activation</groupId>
      <artifactId>activation</artifactId>
      <version>${javax.activation.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <version>${junit.jupiter.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>${junit.jupiter.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>${mockito.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <version>${mockito.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

</project>